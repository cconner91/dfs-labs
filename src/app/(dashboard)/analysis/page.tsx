import { ComingSoon } from "@/components/coming-soon";

export default function AnalysisPage() {
  return (
    <ComingSoon
      title="Result Analysis"
      description="ROI, win rate, and cash rate broken down by GPP vs. Cash and by contest sub-type (H2H, 50/50, Large Field, Milly Maker, etc.) ships once a season of entries has accumulated via the Entries tab."
    />
  );
}
