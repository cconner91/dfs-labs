import { ComingSoon } from "@/components/coming-soon";

export default function TrendsPage() {
  return (
    <ComingSoon
      title="Performance Trends"
      description="Rolling ROI, bankroll growth, and entry volume over time — charted week over week and month over month."
    />
  );
}
