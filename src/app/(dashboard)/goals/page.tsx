import { ComingSoon } from "@/components/coming-soon";

export default function GoalsPage() {
  return (
    <ComingSoon
      title="Goals"
      description="Set season/monthly/weekly targets — bankroll growth, ROI, max allocation per week — and track progress with nudges when you're drifting off plan."
    />
  );
}
